import {StyleSheet} from 'react-native';
import COLORS from '../../Utils/Color';

const styles = StyleSheet.create({
  buttonContainer: {
    paddingVertical: 3,
    backgroundColor: '#f8864a',
    borderRadius: 20,
    marginHorizontal: 3,
    justifyContent: 'center',
    flexDirection: 'row',
    marginTop: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
  buttonText: {
    color: 'white',
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 15,
    textAlign: 'center',
  },
});

export default styles;
